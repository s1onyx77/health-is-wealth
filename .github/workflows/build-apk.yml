name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout Repository
      - name: Checkout Source Code
        uses: actions/checkout@v4

      # 2. Setup Node.js (Removed cache: 'npm' to prevent lockfile error)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3. Setup Java Development Kit (JDK 17 for Android SDK)
      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 4. Setup Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 5. Install Dependencies & Build Web Assets
      - name: Install Project Dependencies
        run: npm install

      - name: Build Web Distributable
        run: npm run build

      # 6. Initialize & Configure Capacitor for Android
      - name: Configure Capacitor & Add Android Platform
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android

          if [ ! -f capacitor.config.json ] && [ ! -f capacitor.config.ts ]; then
            npx cap init "HealthIsWealth" "com.healthiswealth.app" --web-dir dist
          fi

          if [ ! -d "android" ]; then
            npx cap add android
          fi

          npx cap sync android

      # 7. Grant Execution Permission to Gradle Wrapper
      - name: Grant Executable Permission to Gradle
        run: chmod +x android/gradlew

      # 8. Build Debug APK using Gradle
      - name: Build APK with Gradle
        run: |
          cd android
          ./gradlew assembleDebug --stacktrace --no-daemon

      # 9. Upload APK as Artifact
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: health-is-wealth-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7
