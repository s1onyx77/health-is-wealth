name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Java JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Install dependencies
        run: |
          bun install
          bun add -d @capacitor/cli @capacitor/core @capacitor/android

      - name: Build Web Application
        run: bun run build

      - name: Add and Sync Android Project
        run: |
          bunx cap init "HealthIsWealth" "com.health.is.wealth" --web-dir dist || true
          bunx cap add android || true
          bunx cap sync android

      # Build Debug APK instead of Unsigned Release APK
      - name: Build APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      # Upload the signed Debug APK
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/*.apk
